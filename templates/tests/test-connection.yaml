apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "full-stack.fullname" . }}-test-connection"
  labels:
    {{- include "full-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "full-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
